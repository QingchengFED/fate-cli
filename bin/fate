#!/usr/bin/env node

const program = require('commander');

program
  .usage('<command> [options]')
  .version(require('../package').version)
  .command('init', '通过模板创建新项目')
  .command('ng', '符合angular style guide规范的一些快捷方式')
  .parse(process.argv);

