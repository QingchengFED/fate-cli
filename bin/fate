#!/usr/bin/env node

var program = require('commander')

program
	.usage('<command> [options]')
	.version(require('../package').version)
	.command('init', '通过模板创建新项目')
	.command('quick', '一些为了提高开发效率的快捷工具')
	.parse(process.argv)

